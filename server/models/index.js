const Friend = require('./Friends')
const Message = require('./Messages')
const User = require('./User')
const Notifications = require('./Notifications');

module.exports = { Friend, Message, User, Notifications }